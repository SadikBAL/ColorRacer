using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class LevelScrollViewItem : MonoBehaviour
{
    // Start is called before the first frame update
    public Image ButtonImage;
    public TextMeshProUGUI ButtonText;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
